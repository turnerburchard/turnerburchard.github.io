
const box = document.getElementById('landingButton');

const landing = document.getElementById('landing');

box.addEventListener('click', function handleClick(event) {
    // event.target.remove();
    landing.remove()
});
